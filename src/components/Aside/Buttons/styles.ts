import styled from 'styled-components';

export const Button = styled.button`
  background: none;

  border: none;
  color: white;
  font-weight: bold;
  width: 3rem;
  height: 2rem;

  &:hover {
    background: white;
    color: black;
  }
`;